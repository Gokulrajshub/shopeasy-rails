---
trigger: always_on
---

- Always use Ruby on Rails conventions (MVC pattern)
- Use Tailwind CSS for styling
- use bootstrap
- Use explicit individual routes (get, post, patch, delete) 
  instead of resources helper
- Always create migration files for database changes
- Write ERB templates for views
- Use Turbo/Stimulus for interactivity